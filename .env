# FR : Port sur lequel le serveur Express va démarrer
# EN : Port where the Express server will start
PORT=3002

# FR : URI de connexion MongoDB (utilise un utilisateur "appuser" et la base "port_russell")
#      - retryWrites=true : réessaie automatiquement les écritures en cas d’erreur réseau
#      - w=majority : assure que les écritures sont validées par la majorité des nœuds
# EN : MongoDB connection URI (uses user "appuser" and database "port_russell")
#      - retryWrites=true : automatically retries writes on network errors
#      - w=majority : ensures writes are acknowledged by the majority of replica nodes
MONGODB_URI=mongodb+srv://appuser:PortRussell2025@cluster0.snrudpm.mongodb.net/port_russell?retryWrites=true&w=majority
