<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <!-- 
    FR : Définit le titre de la page. 
         Si la variable "title" n’existe pas ou est vide, "Port Russell" est utilisée par défaut.
    EN : Sets the page title. 
         If the variable "title" doesn’t exist or is empty, "Port Russell" is used as fallback.
  -->
  <title><%= (typeof title !== 'undefined' && title) ? title : 'Port Russell' %></title>

  <!-- 
    FR : Feuille de style principale du site (CSS global)
    EN : Main global stylesheet for the site
  -->
  <link rel="stylesheet" href="/styles.css" />
</head>

<body>
  <!-- =========================================================
       FR : En-tête principal avec barre de navigation
       EN : Main header with navigation bar
       ========================================================= -->
  <header class="topbar">
    <nav class="nav">
      <!-- 
        FR : Liens de navigation globaux (Accueil, Dashboard, Docs)
        EN : Global navigation links (Home, Dashboard, Docs)
      -->
      <a href="/">Accueil</a>
      <a href="/dashboard">Tableau de bord</a>
      <a href="/docs" target="_blank" rel="noreferrer">Docs API (Swagger)</a>

      <!-- 
        FR : Affiche l’adresse e-mail de l’utilisateur connecté (si disponible)
        EN : Displays logged-in user’s email (if available)
      -->
      <span class="user">
        <%= (typeof userEmail !== 'undefined' && userEmail) 
              ? ('Connecté : ' + userEmail) 
              : '' %>
      </span>

      <!-- 
        FR : Lien de déconnexion toujours accessible
        EN : Logout link always accessible
      -->
      <a href="/logout" class="btn-logout">Se déconnecter</a>
    </nav>
  </header>

  <!-- =========================================================
       FR : Conteneur principal du contenu de la page
       EN : Main container for the page content
       ========================================================= -->
  <main class="container">
